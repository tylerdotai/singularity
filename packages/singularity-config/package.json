{
	"name": "singularity-config",
	"version": "0.1.0",
	"description": "Singularity config schema and defaults",
	"type": "module",
	"license": "MIT",
	"exports": {
		".": "./src/index.ts",
		"./validation": "./src/validation.ts",
		"./store": "./src/store.ts"
	},
	"scripts": {
		"build": "echo 'singularity-config is a library; build is a no-op' && exit 0",
		"typecheck": "bunx tsc --noEmit --skipLibCheck",
		"lint": "bunx biome check .",
		"lint:fix": "bunx biome check --write ."
	},
	"dependencies": {
		"zod": "^3.24.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.16",
		"@types/node": "^25.9.3"
	}
}
