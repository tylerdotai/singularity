/**
 * singularity-engine — public interface re-exports.
 *
 * Re-export EngineDeps here so callers can import it from "./interfaces"
 * without pulling in the full types module.
 */

export type { EngineDeps } from './types.js';
