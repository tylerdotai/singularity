{
	"name": "singularity-loop",
	"version": "0.1.0-phase-5.1",
	"description": "Singularity closed-loop quality gate engine. Phase 5.1: runLoop engine, WorktreeWorker, and DefaultEvaluator.",
	"private": true,
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"build": "echo 'library only (noEmit: true)' && exit 0",
		"typecheck": "bunx tsc --noEmit --skipLibCheck",
		"lint": "bunx biome check .",
		"lint:fix": "bunx biome check --write .",
		"test": "bun test"
	},
	"dependencies": {},
	"devDependencies": {
		"@types/bun": "latest"
	}
}
