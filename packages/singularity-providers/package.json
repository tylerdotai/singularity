{
	"name": "singularity-providers",
	"version": "0.1.0",
	"description": "Model provider registry and configuration for Singularity",
	"type": "module",
	"license": "MIT",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"build": "echo 'singularity-providers is a library; build is a no-op' && exit 0",
		"typecheck": "bunx tsc --noEmit --skipLibCheck",
		"lint": "bunx biome check .",
		"lint:fix": "bunx biome check --write ."
	},
	"dependencies": {
		"singularity-config": "file:../singularity-config",
		"singularity-llm": "file:../singularity-llm"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.16",
		"@types/node": "^25.9.3"
	}
}
